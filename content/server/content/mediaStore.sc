object mediaStore {
   String inbox; // Where we store these directly from download
   String origDir = "/usr/local/contentRoot/orig"; // After the names have been picked and stored as the originals
   String genDir = "/usr/local/contentRoot/gen"; // Generated files from the originals - contains name.txt file for the info about the file used to generate
}
