// TODO: implement storeScope - pull out the store name from
// the domain, path or a request header.  use that as a new scope
// that can be shared by multiple page apps
scope<session> 
currentOrderView {
}
